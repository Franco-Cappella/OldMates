@{
    ViewData["Title"] = "Notificaciones";
}

<div id="notifications-container">
    <div class="notification invite">
        <span class="text">Juan te ha invitado a “Noche de Burako”</span>
        <div class="actions">
            <button class="mark-read" title="Marcar como leída">✓</button>
            <button class="delete" title="Borrar">🗑️</button>
        </div>
    </div>

    <div class="notification activity">
        <span class="text">Hoy tienes la actividad “Partido de Tejo”</span>
        <div class="actions">
            <button class="mark-read" title="Marcar como leída">✓</button>
            <button class="delete" title="Borrar">🗑️</button>
        </div>
    </div>

    <div class="notification activity">
        <span class="text">Hoy tienes la actividad “Año Nuevo”</span>
        <div class="actions">
            <button class="mark-read" title="Marcar como leída">✓</button>
            <button class="delete" title="Borrar">🗑️</button>
        </div>
    </div>

    <div class="notification invite">
        <span class="text">Marta te ha invitado a “Día de Crochet”</span>
        <div class="actions">
            <button class="mark-read" title="Marcar como leída">✓</button>
            <button class="delete" title="Borrar">🗑️</button>
        </div>
    </div>

    <div class="notification reminder">
        <span class="text">Recuerda confirmar tu asistencia para “Cena de Amigos”</span>
        <div class="actions">
            <button class="mark-read" title="Marcar como leída">✓</button>
            <button class="delete" title="Borrar">🗑️</button>
        </div>
    </div>

    <div class="notification update">
        <span class="text">Nuevo mensaje en “Chat de Servicio”</span>
        <div class="actions">
            <button class="mark-read" title="Marcar como leída">✓</button>
            <button class="delete" title="Borrar">🗑️</button>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const container = document.getElementById('notifications-container');

            container.addEventListener('click', e => {
                if (e.target.classList.contains('mark-read')) {
                    const notification = e.target.closest('.notification');
                    notification.classList.toggle('read');
                }
                if (e.target.classList.contains('delete')) {
                    const notification = e.target.closest('.notification');
                    notification.classList.add('removing');
                    setTimeout(() => notification.remove(), 400);
                }
            });
        });
    </script>
}
